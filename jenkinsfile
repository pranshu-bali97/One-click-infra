pipeline {
    agent any
    environment {
        AWS_ACCESS_KEY_ID = 'AKIAR7HWX7PULNEV6S7L'
        AWS_SECRET_ACCESS_KEY = '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'
    }
    stages {
        stage('Checkout Code') {
            steps {
                checkout scm
            }
        }
        stage('Terraform Init') {
            steps {
                sh 'terraform init'
            }
        }
        stage('Terraform Validate') {
            steps {
                sh 'terraform validate'
            }
        }
        stage('Terraform Plan') {
            steps {
                sh 'terraform plan -out=tfplan'
            }
        }
        stage('Terraform Apply') {
            steps {
                input message: 'Apply changes?'
                sh 'terraform apply -auto-approve tfplan'
            }
        }
    }
}
